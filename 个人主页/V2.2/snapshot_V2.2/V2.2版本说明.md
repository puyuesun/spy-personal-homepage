# V2.2 版本说明 — 孙璞月 Spy 个人主页

> **版本号**：V2.2
> **完成时间**：2026-09-14（同日及 2026-09-17 有 V2.2 内微调：高亮发光、高亮亮度回调、全站玻璃拟态）
> **基线版本**：V2.1
> **本版本主题**：**首屏以外的全部区域**新增 Magic UI **Interactive Grid Pattern（交互式网格背景）**——指针落在哪一格，哪一格就填充克莱因蓝高亮，移开后缓慢淡出；首屏保持 V2.1 原样不变。另将全站所有「半透明框」统一改造为**玻璃拟态（Glassmorphism）**
> **技术栈**：原生 HTML / CSS / JavaScript（零第三方依赖，可继续用 GitHub Pages 发布）

---

## 1. 本版本做了什么

一件事：**给首屏以外的所有区域铺上「会跟着鼠标亮起来的网格背景」**。

- 从「关于我」到页脚，整个页面背景是一张 56px 的网格
- **指针停在哪一格，那一格就被填充成克莱因蓝**
- 指针移开后，那一格**缓慢淡出**（约 1 秒），不是一下消失
- **首屏 Hero 完全没有变**：仍是 V2.1 的 3D 粒子星场 + 静态网格，指针在首屏内移动不会触发任何网格高亮
- 效果来源：Magic UI `Interactive Grid Pattern`，按官方源码**等价移植为原生 SVG + JS**，零依赖

三项要求全部满足：

| 要求 | 实现情况 |
|---|---|
| 用 Magic UI 的 Interactive Grid Pattern | ✅ 按官方源码等价移植（官方为 React + SVG） |
| 应用在**除了 hero 以外**的部分 | ✅ 网格层自 `--hero-h`（首屏高度）开始，一直到页面底部 |
| hero 不受影响 | ✅ 首屏仍是粒子 + 静态网格；实测指针在首屏内 0 格高亮 |

另有一项同版本内完成的改造：**把页面上所有「半透明框」统一换成玻璃拟态（Glassmorphism）**。

- 玻璃 = 半透明底色 + **背后模糊**（`backdrop-filter`）+ 1px 浅色描边 + 上沿内高光 + 柔和外阴影
- 覆盖 **18 个内容卡片**（自我介绍卡、4 张信息卡、3 张技能卡、3 张项目卡、奖项条、3 张兴趣卡、3 张联系卡）
  + **顶部导航条**，以及各类小控件：首屏标签、状态胶囊、次按钮、复制按钮、兴趣图标块、占位标签、头像占位面板
- 观感取向上是**深色磨砂**而非浅色玻璃：底色仍是深蓝黑（`rgba(12, 19, 40, 0.50)`），
  只透出背后的网格线与蓝色光晕，**保证卡片上的文字对比度不受影响**
- 老浏览器不支持 `backdrop-filter` 时会自动回退成「更实的半透明底色」，不会退化成几乎全透明而看不清字

---

## 2. 关于「官方 CLI」与落地方式的说明（重要）

官方安装方式是：

```bash
pnpm dlx shadcn@latest add @magicui/interactive-grid-pattern
```

**实际情况**：该命令面向 **React + Tailwind + shadcn/ui** 工程，执行后生成
`components/ui/interactive-grid-pattern.tsx`（React 组件）。本项目是**纯静态 HTML/CSS/JS**，
没有 React 工程与构建流程，直接执行 CLI 无法产出可被本页面引用的文件。

因此沿用 V2.1 已确立的做法 —— **原生等价移植**：

| 项目 | 说明 |
|---|---|
| 官方实现 | React 组件 + 内联 SVG 网格 + Tailwind 工具类 |
| 官方源码 | `registry/magicui/interactive-grid-pattern.tsx` |
| 获取方式 | 从官方 registry 直接取回源码：`https://magicui.design/r/interactive-grid-pattern.json` |
| 本版本做法 | 按官方源码等价改写为原生 SVG + 原生 JavaScript，行为与参数体系一致，零依赖 |

> 官方文档页：https://magicui.design/docs/components/interactive-grid-pattern

### 2.1 官方原理解读

官方组件的核心是**三件事**：

1. **画网格**：一个 `<svg>` 里铺满 `width × height` 的 `<rect>` 方格
   （官方默认 `width=40, height=40, squares=[24,24]`，即 24×24 个 40px 方格）
2. **记录指针在哪一格**：React `useState` 保存 `hoveredSquare: [x, y]`，
   指针经过某个 `rect` 时把它的坐标写进 state
3. **高亮那一格 + 慢速淡出**：被选中的 `rect` 填充高亮色；官方用的是 Tailwind 类
   `transition-all duration-100 ease-in-out not-[&:hover]:duration-1000`
   —— **进入时 100ms 快速填充，离开时 1000ms 慢速淡出**

### 2.2 本项目的等价改写

| 官方做法 | 本版本做法 | 原因 |
|---|---|---|
| React `useState` 记录 hoveredSquare | 指针坐标反推格子索引 + 给对应 `<rect>` 切换 `.is-hover` 类 | 无 React |
| 固定 24×24 个 40px 方格 | 按容器尺寸动态计算行列数（26 列 × 57 行 = 1482 格），方格 56px | 需铺满整个页面区域，且要与首屏已有的 56px 网格对齐 |
| 在 `rect` 上用 `:hover` 触发 | 在 `window` 上监听 `pointermove`，用坐标换算定位 | **关键**：网格层是 `pointer-events: none`（避免挡住卡片点击与文字选择），此时 `rect` 收不到鼠标事件，官方的 `:hover` 方案在本页面**根本不会触发** |
| 高亮色 `gray-300/30` | 克莱因蓝 `rgba(47, 107, 255, 0.2)` + 描边提亮 + 两层辉光 | 贴合本页主色调；并按后续反馈调整过亮度（见 4.6） |
| 100ms 进入 / 1000ms 离开 | 完全一致（CSS `transition: fill, stroke, filter 1s` + `.is-hover` 时 `transition-duration: .1s`） | 保持官方手感，发光也一起快进慢出 |

> 也就是说：**交互手感与官方一致（快进慢出、指针所在格高亮），但因为是静态页面 + 网格层不拦截事件，必须改用坐标换算而不是 CSS `:hover`。**

---

## 3. 可调参数（交互网格 + 玻璃拟态）

### 3.1 交互网格（`script.V2.2.js` 的 `CONFIG` 与 `style.V2.2.css` 的 `.igrid` 段）

`script.V2.2.js` 第 6 节顶部：

| 参数 | 取值 | 官方默认 | 说明 |
|---|---|---|---|
| `cell` | `56` | `40` | 方格边长 px；取 56 是为了与首屏静态网格 `background-size: 56px 56px` 对齐，整页网格观感统一 |
| `enabled` | `true` | — | 交互总开关；设 `false` 后网格照常显示，但不再跟随指针高亮 |

行数 / 列数不是固定值，而是**按容器实际尺寸算出来**的：`cols = ceil(宽 / 56)`、`rows = ceil(高 / 56)`。
实测 1440×1000 视口下为 **26 列 × 57 行 = 1482 格**。

高亮与线条样式在 `style.V2.2.css` 的 `.igrid-layer` / `.igrid rect` 段：

| 位置 | 取值 | 官方默认 | 说明 |
|---|---|---|---|
| 线条颜色 | `rgba(47, 107, 255, 0.05)` | `border-gray-400/30` | 沿用首屏静态网格的克莱因蓝线条风格 |
| 线条宽度 | `1px` | `1px` | 一致 |
| 默认填充 | `transparent` | `transparent` | 只有描边，不填充 |
| 高亮填充 | `rgba(47, 107, 255, 0.2)` | `gray-300/30` | 改为克莱因蓝，贴合主色调；亮度见 4.6 的调整记录 |
| 高亮描边 | `rgba(110, 160, 245, 0.55)` | 与默认同（`gray-400/30`） | 高亮时描边一并提亮，让格子轮廓更清晰 |
| 高亮发光 | `drop-shadow(0 0 6px rgba(110,165,255,0.55)) drop-shadow(0 0 16px rgba(47,107,255,0.32))` | 无 | **本版本新增**：两层辉光（内层紧贴格子的亮晕 + 外层大范围柔光） |
| 进入过渡 | `0.1s` | `100ms` | 一致（fill / stroke / filter 三个属性一起快速点亮） |
| 离开过渡 | `1s` | `1000ms` | 一致（fill / stroke / filter 三个属性一起缓慢淡出） |

### 3.2 玻璃拟态（`style.V2.2.css` 的 `:root` `--glass-*` 令牌）

所有玻璃面共用同一套令牌，**改一处即可全站生效**：

| 令牌 | 当前值 | 说明 |
|---|---|---|
| `--card` | `rgba(12, 19, 40, 0.50)` | 玻璃底色：半透明深蓝黑，透出背后的网格与光晕 |
| `--card-hover` | `rgba(20, 33, 66, 0.56)` | 悬停底色：略微提亮 |
| `--glass-sheen` | `linear-gradient(150deg, rgba(255,255,255,0.09), rgba(255,255,255,0.015) 38%, rgba(255,255,255,0.05))` | 斜向高光渐变，玻璃的「质感层」 |
| `--glass-border` | `rgba(255, 255, 255, 0.14)` | 1px 浅色描边 |
| `--glass-border-hover` | `rgba(94, 150, 255, 0.52)` | 悬停描边：转为蓝调 |
| `--glass-inner` | `inset 0 1px 0 rgba(255, 255, 255, 0.18)` | 上沿内高光，玻璃的「厚度」 |
| `--glass-shadow` | `0 18px 44px rgba(2, 5, 16, 0.50)` | 外阴影，把玻璃面从背景中「托起来」 |
| `--glass-shadow-hover` | `0 26px 60px rgba(0, 47, 167, 0.42)` | 悬停外阴影：克莱因蓝投影 |
| `--glass-blur` | `18px` | 卡片 / 导航条模糊半径 |
| `--glass-blur-sm` | `10px` | 小控件模糊半径（小面积不需要大模糊，省性能） |
| `--glass-sat` | `150%` | 提高背后颜色的饱和度，让蓝更透 |

| 想要的效果 | 改哪个 |
|---|---|
| 玻璃更透 / 更实 | `--card` 的 alpha（`0.50`；减小更透，增大更实） |
| 模糊更强 / 更弱 | `--glass-blur`（`18px`）、小控件改 `--glass-blur-sm`（`10px`） |
| 描边更亮 / 更淡 | `--glass-border`（`0.14`） |
| 高光更明显 / 更收敛 | `--glass-sheen` 里三处 `rgba(255,255,255,…)` 的 alpha |
| 阴影更重 / 更轻 | `--glass-shadow`、`--glass-shadow-hover` |
| 悬停时的蓝光更重 / 更轻 | `--glass-border-hover` 与 `--glass-shadow-hover` 的 alpha |
| 完全不想要玻璃（回到 V2.1 的实色卡片） | 删掉 `.card` 里的 `backdrop-filter` 与 `--glass-sheen`，把 `background` 改回 `rgba(255,255,255,0.035)` |

> 有两个**特意不加模糊**的例外，改的时候不用找：
> `.nav-link` 是嵌在「已经磨砂的导航条」上的小玻璃片，再叠一层模糊几乎无视觉收益、只增合成开销；
> `.skill-bar` 是内凹的进度槽（凹槽），不是悬浮玻璃面。

---

## 4. 实现细节与踩坑记录

### 4.1 网格层只覆盖首屏以外

新增一个装饰层 `.igrid-layer`：

```css
.igrid-layer {
  position: absolute;
  left: 0; right: 0;
  top: var(--hero-h, 100vh);   /* ← 自首屏下沿开始 */
  bottom: 0;                   /* ← 一直到页面底部（含页脚） */
  z-index: -2;
  overflow: hidden;
  pointer-events: none;
}
```

`--hero-h` 由 `script.V2.2.js` 实测首屏高度后写入 `:root`（实测 907px），
并用 `ResizeObserver` 盯住 `.hero`，窗口尺寸变化时同步更新。

同时把**首屏原来的静态网格 `.bg-grid` 从「铺满整个视口」改为「只覆盖首屏」**
（`position: fixed; inset: 0` → `position: absolute; height: var(--hero-h)`）。
这样两套网格各管一段、互不重叠，首屏观感与 V2.1 完全一致。

> 注意：`body` 需要 `position: relative`，否则这两个绝对定位层会相对初始包含块定位而不是页面。

### 4.2 网格线与首屏对齐

首屏静态网格从页面顶部起按 56px 排布，而交互网格层是从 `hero 下沿` 开始算坐标的。
如果直接从 0 开始铺，两套网格线在交界处会**错开半个格子**。

处理：计算偏移量 `offsetY = 首屏高度 % 56`（实测 `907 % 56 = 11`），
让第一行方格上移 `offsetY`，于是两套网格的线条在交界处**严丝合缝**接上。
第一行被裁掉的上半截由 `.igrid-layer { overflow: hidden }` 吃掉。

### 4.3 为什么不用 CSS `:hover`

网格层必须 `pointer-events: none`（否则会挡住卡片上的按钮点击与文字选择），
而 `pointer-events: none` 的元素**收不到** `:hover`。
所以改为在 `window` 上监听 `pointermove`，用 `clientX/Y` 与网格层的位置反推格子索引。

### 4.4 性能处理

- **位置缓存**：网格层的 `getBoundingClientRect()` 只在生成网格、滚动、窗口变化时测量一次并缓存；
  指针移动时直接用缓存值算坐标，**不会触发强制重排**
- **只动两个节点**：切换高亮时只移除上一格的类、添加新一格的类，其余 1480 个 `rect` 不碰
- **不做逐帧重算**：浏览器本身已把 `pointermove` 按帧合并，无需再排队到 `requestAnimationFrame`

### 4.5 滚动后的高亮校正

页面滚动后网格层相对视口的位置变了，若指针不动，高亮会**停在旧格子上**。

处理：记录最近一次指针位置，滚动时刷新位置缓存并**用最近的指针坐标重新定位**
（而不是直接清掉高亮——直接清掉会出现「滚动事件晚于鼠标事件」时把刚点亮的一格误清掉的竞态）。

### 4.6 高亮的可见度增强与发光（V2.2 内微调，含一次亮度回调）

高亮效果在本版本内经过**两轮**调整，都是按反馈在同一版本内微调：

**第一轮 —— 让高亮更明显并带发光**

初版高亮沿用官方 `gray-300/30` 的量级（克莱因蓝 `0.18`）。实测在深色背景上偏含蓄，
因此做了增强：

| 手段 | 改动 |
|---|---|
| 填充调浓 | `rgba(47, 107, 255, 0.18)` → `rgba(47, 107, 255, 0.32)` |
| 描边提亮 | 高亮时描边由 `rgba(47, 107, 255, 0.05)` → 亮蓝，格子轮廓更清晰 |
| 加两层辉光 | `drop-shadow(0 0 6px …)` + `drop-shadow(0 0 16px …)` |
| 发光也参与过渡 | `transition` 的属性列表由 `fill` 扩展为 `fill, stroke, filter`，「快进 0.1s / 慢出 1s」的手感不变 |

**第二轮 —— 亮度调低（当前生效值）**

增强后实测格子内部平均亮度达 **98.70**（未高亮时仅 14.58），观感偏亮、偏刺眼，
因此按反馈把填充、描边、两层辉光的 alpha 一并收敛，**保留发光但整体压暗**：

| 手段 | 调整前 | **调整后（当前）** |
|---|---|---|
| 填充 | `rgba(47, 107, 255, 0.32)` | **`rgba(47, 107, 255, 0.2)`** |
| 描边 | `rgba(122, 176, 255, 0.85)` | **`rgba(110, 160, 245, 0.55)`** |
| 内层辉光（6px） | `rgba(110, 165, 255, 0.95)` | **`rgba(110, 165, 255, 0.55)`** |
| 外层辉光（16px） | `rgba(47, 107, 255, 0.6)` | **`rgba(47, 107, 255, 0.32)`** |

两层辉光的分工：**内层 6px** 紧贴格子形成亮晕（让格子看起来在「发光」），
**外层 16px** 向外扩散成柔和光斑（让光溢出格子边界，与周围环境产生关系）。

**实测效果对比**（同一格，指针移开并完全淡出后对比）：

| 指标 | 初版（无发光） | 第一轮（偏亮） | **第二轮（当前）** | 未高亮时 |
|---|---|---|---|---|
| 格子内部平均亮度 | 36.01 | 98.70 | **58.50** | 14.58 |
| 格子内部平均色 | — | RGB(44, 85, 168) | **RGB(25, 50, 100)** | RGB(6, 14, 24) |
| 格子**外圈**平均亮度 | — | 28.93 | **19.96** | 15.54 |

> 内部平均色 RGB(25, 50, 100) 说明高亮是**饱和的深蓝**，不是被辉光冲淡的灰蓝：
> 蓝通道约为红通道的 4 倍。
>
> 「外圈亮度」是发光的客观证据：格子外部本来没有填充，那圈区域变亮只可能来自辉光
> —— 当前外圈比未高亮时亮 **+4.42**，即**发光仍然存在，只是收敛得更含蓄**。
>
> 调法见 `.igrid rect.is-hover`：想让光更弱就调小两个 `drop-shadow` 的 alpha，
> 想让光更大就调大模糊半径（第一个值）。

### 4.7 无障碍

- 系统开启「减少动态效果」时，给 `<html>` 加 `.igrid-no-motion`，**取消填充过渡**，
  高亮改为瞬时切换（保留反馈，去掉动效）
- 整个网格层是 `aria-hidden="true"` 的纯装饰层，不进入无障碍树
- `pointer-events: none`，不影响键盘操作与文字选择

### 4.8 玻璃拟态的实现要点

**为什么不是「画个半透明矩形」**：真正的玻璃必须**模糊背后的内容**才成立，所以每个玻璃面都带
`backdrop-filter: blur(...) saturate(150%)`，并同时给出 1px 浅色描边、上沿内高光（`inset 0 1px 0`）
与柔和外阴影——四件套齐全，玻璃面才会从背景里「浮起来」而不是「贴上去」。同时又都保留 `-webkit-`
前缀（Safari 长期只认带前缀的写法）。

**分两档模糊半径**：卡片与导航条用 `--glass-blur: 18px`（面积大、需要明显磨砂感），
小控件（标签、胶囊、按钮、图标块）用 `--glass-blur-sm: 10px`——小面积给大模糊几乎看不出差别，
却同样要付出合成开销。

**特意不加模糊的两处**：

| 元素 | 原因 |
|---|---|
| `.nav-link`（导航项） | 它嵌在**已经磨砂的导航条**上，再叠一层 `backdrop-filter` 视觉上几乎无变化，只多一个合成层；改用「渐变高光 + 描边」表达玻璃感。另给基类预留 `border: 1px solid transparent`，避免 `.active` 时突然多出 1px 描边造成文字跳动 |
| `.skill-bar`（进度槽） | 它是**内凹的凹槽**而不是悬浮玻璃面，改用 `inset` 内阴影表达「凹」，与外凸的玻璃卡片形成层次 |

**头像占位面板的特例**：它背后是 `.avatar-frame` 的**蓝色渐变**（不透明），玻璃化后若用通用底色，
中间的「SP」文字对比度会下降，因此单独用更深的半透明底 `rgba(10, 19, 48, 0.78)`。

**必须保持不透明的元素**：`.shimmer-backdrop` 不能玻璃化——它的职责就是**遮住** Shimmer 按钮的扫光带
（`background: var(--bg)`），一旦半透明，光带会从按钮里漏到外面。

**老浏览器回退**：不支持 `backdrop-filter` 时，模糊失效会让玻璃退化成几乎全透明、文字看不清，
因此加了 `@supports not ((backdrop-filter: blur(2px)) or (-webkit-backdrop-filter: blur(2px)))` 分支，
把各类玻璃面的底色换成更实的版本（如卡片 `rgba(11, 17, 34, 0.94)`），**可读性优先**。

**没有伤到既有装饰**：`.about-card` 左侧 3px 克莱因蓝装饰条是 `::before` 绘制的，
与玻璃底色互不干扰（已实测仍然存在）。

---

## 5. 文件清单与内容状态

| 文件 | 说明 | 内容状态 |
|---|---|---|
| `index.V2.2.html` | 页面结构（新增 `.igrid-layer` 交互网格层；首屏静态网格注释说明；版本号更新） | 结构真实；头像/项目/奖项/GitHub/简历仍为**占位** |
| `style.V2.2.css` | 页面样式（首屏静态网格改为只覆盖首屏 + 新增交互网格样式段 + **全站玻璃拟态令牌与玻璃配方**） | 真实 |
| `script.V2.2.js` | 页面交互 + 首屏粒子引擎 + **交互网格模块（第 6 节）** | 真实 |
| `V2.2版本说明.md` | 本文件 | 真实 |
| `V2.2已实现功能.md` | 功能清单 | 真实 |
| `screenshot_V2.2_hero.png` | **首屏截图**（1440 × 1000） | 真实 |
| `screenshot_V2.2_full.png` | **整页截图**（1416 × 4059） | 真实 |
| `screenshot_V2.2_grid.png` | **内容区交互网格截图**（1440 × 1000，含指针高亮的那一格） | 真实 |
| `snapshot_V2.2\` | **本版本快照**：上述 8 个文件的基线副本（2026-09-14 首次创建；2026-09-17 按用户要求**重建为含玻璃拟态的最新状态**，逐文件 `sha256` 校验一致） | 真实 |
| `snapshot_V2.2_pre-glass\` | **玻璃拟态改造之前的基线快照**（2026-09-14 状态，2026-09-17 重建快照时备份保留）：用于回看 / 回滚卡片玻璃化之前的样子 | 真实 |

> 本版本已按用户要求创建快照：2026-09-14 首次创建，2026-09-17 按用户要求**先备份旧快照、再重建**。
> `snapshot_V2.2\` 内容 = 表格中除两个快照目录以外的 8 个文件，
> 即**含「交互网格发光 + 高亮亮度回调 + 全站玻璃拟态」最新调整**的状态；
> 玻璃化之前的基线另存于 `snapshot_V2.2_pre-glass\`（同为 8 个文件），两者可随时对照回看。

**仍为占位、待你提供素材的内容**（与 V2.1 相同，本版本未处理）：

- 头像真实照片
- 项目 01 / 02 / 03 的真实内容
- 奖项 / 荣誉
- GitHub 链接
- 简历 PDF

---

## 6. 与上一版本（V2.1）相比的改动

### 6.1 新增

| 编号 | 改动 | 涉及文件 |
|---|---|---|
| 1 | 新增**「首屏以外区域的交互式网格背景」**：整页背景网格，指针所在格填充克莱因蓝高亮、移开缓慢淡出（Magic UI `Interactive Grid Pattern` 原生移植） | `index.V2.2.html`、`style.V2.2.css`、`script.V2.2.js` |
| 2 | HTML 新增装饰层 `<div class="igrid-layer"><svg id="interactiveGrid"></svg></div>`（`aria-hidden="true"`） | `index.V2.2.html` |
| 3 | CSS 新增 `.igrid-layer` / `.igrid` / `.igrid rect` / `.igrid rect.is-hover` / `.igrid-no-motion` 样式段 | `style.V2.2.css` |
| 4 | JS 新增第 6 节「交互式网格」模块：按容器尺寸生成方格、指针坐标定位、滚动后重新定位、减弱动效适配、调试接口 `window.__interactiveGrid` | `script.V2.2.js` |
| 5 | 新增 CSS 变量 `--hero-h`（首屏实测高度，脚本写入 `:root`），供两个背景层定位 | `script.V2.2.js`、`style.V2.2.css` |
| 6 | 新增**全站玻璃拟态令牌组** `:root --glass-*`：玻璃底色 / 斜向高光渐变 / 浅色描边 / 上沿内高光 / 两层阴影 / 两档模糊半径（18px、10px）/ 饱和度（150%） | `style.V2.2.css` |
| 7 | 新增 `@supports not (backdrop-filter…)` **实色回退分支**：老浏览器上把玻璃面换成更实的底色，保证文字可读 | `style.V2.2.css` |

### 6.2 修改

| 编号 | 改动 | 说明 |
|---|---|---|
| 1 | **首屏静态网格 `.bg-grid` 由「铺满整个视口」改为「只覆盖首屏」** | 原为 `position: fixed; inset: 0`，现为 `position: absolute; top/left/right: 0; height: var(--hero-h)` 并加 `pointer-events: none`。首屏观感完全不变，首屏以外的背景交给新的交互网格层 |
| 2 | `body` 增加 `position: relative` | 让上述绝对定位的背景层相对页面而非初始包含块定位 |
| 3 | 页面标题、页脚版本标识（两处）、样式与脚本引用文件名 | 全部由 V2.1 更新为 V2.2 |
| 4 | CSS 与 JS 头部注释 | 补充 V2.2 新增模块说明 |
| 5 | **高亮可见度增强并追加发光**（V2.2 内微调） | `.igrid rect.is-hover` 填充由 `0.18` 起步增强、描边提亮、新增两层 `drop-shadow` 辉光；`.igrid rect` 的过渡属性由 `fill` 扩展为 `fill, stroke, filter`，使发光也走「快进慢出」 |
| 6 | **高亮亮度回调**（V2.2 内微调） | 增强后实测偏亮，按反馈把填充、描边、两层辉光的 alpha 一并收敛为当前值：填充 `0.2`、描边 `rgba(110,160,245,0.55)`、辉光 `0.55` / `0.32`。内部平均亮度由 98.70 降到 58.50，发光保留（外圈 +4.42） |
| 7 | **全站「半透明框」改造为玻璃拟态**（V2.2 内微调） | 18 个 `.card` + 顶部 `.nav` + 各类小控件（`.tag`、`.avatar-status`、`.btn-ghost`、`.copy-btn`、`.interest-emoji`、`.placeholder-tag`、`.avatar-placeholder`、`.skill-bar`）统一换上「半透明底 + `backdrop-filter` 模糊 + 浅色描边 + 上沿内高光 + 柔和外阴影」的玻璃配方；卡片悬停描边转蓝调、外阴影转为克莱因蓝投影。`.shimmer-backdrop` 按设计**保持不透明** |

### 6.3 移除

- 无。V2.1 的首屏 3D 粒子背景、鼠标推开粒子、「联系我」Shimmer 按钮、光晕层**全部完整保留**。

---

## 7. 验证记录

验证脚本：`.deepworks\tmp\shoot_v22.py`（可重复执行）

**页面与既有功能（确认 V2.1 未回退）**

| 检查项 | 结果 |
|---|---|
| `index.V2.2.html` / `style.V2.2.css` / `script.V2.2.js` 本地加载 | 均 HTTP 200 ✅ |
| 浏览器控制台 JS 异常 / error 日志 | 0 条 ✅ |
| 首屏画布是否仍绘制粒子 | 非透明像素约 1.1 万（**粒子位置每次随机**，多次实测 10,969–11,552），粒子总数仍为 520 ✅ |
| 粒子是否仍在运动 | 两次采样像素数不同（如 11,490 → 11,552、10,969 → 11,042），确认逐帧变化 ✅ |
| Hero 是否仍铺满视口宽 | Hero 1416px > 内容 1140px ✅ |

**背景分层**

| 检查项 | 结果 |
|---|---|
| `--hero-h` 是否由脚本写入 | `907px`，与实际首屏高度一致 ✅ |
| 首屏静态网格是否只覆盖首屏 | 高度 907px（= 首屏高度），不再铺满全页 ✅ |
| 首屏静态网格样式是否保持原样 | 仍为 `56px 56px` 渐变线条 ✅ |
| 交互网格起点是否为首屏下沿 | `top = 907px` ✅ |
| 交互网格是否覆盖到页面底部 | 907 + 3152 = 4059 = 页面总高 ✅ |
| 网格层是否不挡点击 | `pointer-events: none` ✅ |

**交互式网格结构与参数**

| 检查项 | 结果 |
|---|---|
| 方格是否生成 | 26 列 × 57 行 = **1482 格**（SVG 内 `rect` 数量一致）✅ |
| 方格尺寸 | 56 × 56 px ✅ |
| 默认填充 | 透明（仅描边）✅ |
| 线条 | `rgba(47, 107, 255, 0.05)` / 1px ✅ |
| 离开过渡是否挂载 | `fill, stroke, filter 1s` ✅ |
| 未交互时的高亮数 | 0 格 ✅ |
| 与首屏网格是否对齐 | `offsetY = 11px`（= 907 % 56）✅ |

**指针交互**（用 CDP 发送真实鼠标事件）

| 检查项 | 结果 |
|---|---|
| 指针在首屏内（720, 520） | 高亮 0 格，`hovered = -1` ✅ |
| 指针在内容区（30, 150） | 高亮 **恰好 1 格**，索引 286（第 0 列第 11 行）✅ |
| 高亮填充色 | `rgba(47, 107, 255, 0.2)` 克莱因蓝 ✅ |
| 高亮描边 | 提亮为 `rgba(110, 160, 245, 0.55)` ✅ |
| 是否应用发光 | 计算样式 `filter` = `drop-shadow(…0 0 6px) drop-shadow(…0 0 16px)` ✅ |
| 发光是否参与过渡 | 过渡属性为 `fill, stroke, filter`，进入 `0.1s` / 离开 `1s` ✅ |
| 高亮在像素上是否真的可见 | 该格内部亮度 **14.58 → 58.50**（Δ43.92）✅ |
| 亮度是否已按反馈调低 | 由增强后的 98.70 降到 **58.50**（约 −41%）✅ |
| 高亮是否被发光冲成灰蓝 | 高亮时内部平均色 RGB(25, 50, 100)，蓝通道约为红通道 4 倍，饱和深蓝、未发灰 ✅ |
| 发光是否真的溢出格子边界 | 格子**外圈**（外扩 18px 环带）亮度 **15.54 → 19.96**（Δ4.42）✅ |
| 指针移开后是否清除 | 高亮类即时移除 ✅ |
| 淡出是否为渐变（非瞬变） | 填充 alpha 采样 `0.2 → 0.16`（0.18s）→ `0`（1.6s）✅ |
| 离开后是否恢复慢速淡出 | 基础过渡回到 `fill, stroke, filter 1s`，发光一并淡出 ✅ |

**玻璃拟态（Glassmorphism）**

| 检查项 | 结果 |
|---|---|
| 18 个 `.card` 是否全部玻璃化 | 18 / 18 ✅ |
| 卡片的玻璃配方 | 计算样式 `backdrop-filter: blur(18px) saturate(1.5)` ✅ |
| 导航条是否玻璃化 | `.nav` = `blur(18px) saturate(1.5)`，滚动后描边 + 阴影加深 ✅ |
| 小控件是否玻璃化 | `.tag`(3) / `.avatar-status`(1) / `.btn-ghost`(1) / `.copy-btn`(1) / `.interest-emoji`(3) / `.placeholder-tag`(3) / `.avatar-placeholder`(1) 均为 `blur(10px) saturate(1.5)` ✅ |
| 是否有遗漏未加模糊的玻璃控件 | 无。两处不含模糊是**有意的设计取舍**：`.nav-link`（嵌在已磨砂的导航条上）、`.skill-bar`（内凹凹槽）✅ |
| **模糊是否真的在采样并改写背后内容** | 同区域开关 `backdrop-filter` 的像素差：平均 **1.63**、最大 **27**、**87.1%** 的像素发生变化 ✅ |
| 是否伤到既有装饰 | `.about-card` 左侧 3px 克莱因蓝装饰条（`::before`）仍在 ✅ |
| 是否引入 JS 异常 | 控制台 0 条 ✅ |

> 淡出采样与官方 `duration-1000` 的 1 秒慢速淡出曲线一致。

截图为 Edge 无头模式渲染实拍，非示意图。

---

## 8. 如何预览

1. 直接双击 `个人主页\V2.2\index.V2.2.html`，**把鼠标移到「关于我」以下的任何位置**——
   指针所在的那一格会亮起来，移开后约 1 秒淡出
2. 或重新跑验证 + 截图脚本：

```cmd
cd /d "D:\天大港理工深圳学院\AI&Project"
D:\anaconda3\python.exe ".deepworks\tmp\shoot_v22.py"
```

---

## 9. 下一步（V2.3 及之后）

- [x] 让高亮更明显并带发光（**已在 V2.2 内完成**：填充调浓、描边提亮、两层 `drop-shadow` 辉光）
- [x] 高亮亮度调低（**已在 V2.2 内完成**：填充 0.32 → 0.2、两层辉光 alpha 0.95/0.6 → 0.55/0.32，
      内部亮度 98.70 → 58.50，发光保留）
- [ ] 发光强度 / 范围再微调（改 `.igrid rect.is-hover` 的两个 `drop-shadow` 的 alpha 与模糊半径）
- [ ] 网格观感微调：方格大小（`CONFIG.cell`）、线条浓淡、高亮色的透明度
      （改 `style.V2.2.css` 的 `.igrid rect` / `.igrid rect.is-hover`）
- [x] 全站「半透明框」改为玻璃拟态（**已在 V2.2 内完成**：18 张卡片 + 导航条 + 标签 / 胶囊 / 按钮 / 图标块 / 占位标签 / 头像占位面板）
- [ ] 玻璃强度微调（改 `:root` 的 `--card` 透明度、`--glass-blur` 模糊半径、`--glass-sheen` 高光；小控件改 `--glass-blur-sm`）
- [ ] 是否改成「浅色玻璃」（当前为**深色磨砂**；若改浅色需要同步调整卡片内文字颜色，避免对比度不足）
- [ ] 高亮格是否加圆角
- [ ] 是否在首屏内也用交互网格（当前按要求**只在首屏以外**）
- [ ] 首屏粒子的密度 / 速度按你的观感微调（改 `script.V2.2.js` 的 `CONFIG`）
- [ ] 鼠标推开粒子的「幅度 / 快慢」按你的观感微调（`CONFIG.interact` 的 `displace` / `ease`）
- [ ] Shimmer 按钮的扫光宽度 / 颜色 / 周期按观感微调（改 `.btn-shimmer` 的 CSS 变量）
- [ ] 顶部导航栏的「联系我」按钮是否也换成 Shimmer 样式
- [ ] 深色 / 浅色模式切换按钮（V2 计划项）
- [ ] 中英双语切换按钮（V2 计划项，当前为对照展示）
- [ ] 卡片入场动画等微交互
- [ ] 补齐头像、项目、奖项、GitHub、简历等真实素材
